# Frontend Environment Configuration
# Copy this file to .env for local development

# Disable CI build warnings (for development)
CI=false

# API URL Configuration (OPTIONAL - Auto-detected by default!)
# The app automatically detects the correct API based on where it's running:
# - localhost -> http://localhost:3005
# - playsluff.com -> https://api.playsluff.com
# - staging/preview -> staging API
# - other deployments -> production API
#
# Only uncomment if you need to override auto-detection:
# REACT_APP_SERVER_URL=http://localhost:3005

# Other optional environment variables:
# REACT_APP_DEBUG=true
# REACT_APP_WEBSOCKET_URL=ws://localhost:3005